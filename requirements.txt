# Core vision & math
numpy>=1.26
opencv-contrib-python>=4.8

# Pygame visualization window
pygame>=2.6

# Game environment (Gymnasium interface)
gymnasium>=1.0

# AI agents — RL models
stable-baselines3>=2.0
sb3-contrib>=2.0        # MaskablePPO (used by HybridAlphaZero agent)
torch>=2.0

# Android device control
adbutils>=2.0
scrcpy>=1.0             # py-scrcpy-client — install with: pip install scrcpy
